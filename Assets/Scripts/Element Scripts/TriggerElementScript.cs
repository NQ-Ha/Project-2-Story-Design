using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerElementScript : MonoBehaviour
{
    [Header("Trigger Magics")]
    public bool triggerFire;
    public bool triggerWater;
    public bool triggerElectric;
    public bool triggerNature;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
